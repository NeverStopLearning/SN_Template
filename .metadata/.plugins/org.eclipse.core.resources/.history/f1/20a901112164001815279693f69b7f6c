import { TEST_DISPATCH } from '../reducers/types.js';

const initialState = {
		isAuthenticated: false,
		user:{}
}

export default function(state = initialState, action){
	console.log("action: ", action);
	switch(action.type){
		
		default:
			return state;
	}
}

